<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Teacher Grouping Page</title>
    <link rel="stylesheet" href="../public/css/teacher-initial.css">
    <style>
        /* Simple styles for modal and buttons */
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.4);
        }

        .modal-content {
            background-color: #fefefe;
            margin: 15% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 80%;
            max-width: 600px;
        }

        button {
            margin-top: 10px;
            cursor: pointer;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Welcome, <%= teacher.name %>!</h1>
        <p>Email: <%= teacher.email %></p>
        <!-- Teacher-specific data and options go here -->
        <a href="/logout" class="btn">Logout</a>
    </div>
    
    <header>
        <h1>Teams</h1>
    </header>
    <main>
        <div id="team-list">
            <!-- Created teams will appear here -->
        </div>
        <button id="create-team">Create Team</button>
    </main>

    <!-- Popup for displaying team members -->
    <div id="team-modal" class="modal">
        <div class="modal-content">
            <span id="close" class="close" aria-label="Close modal">&times;</span>
            <h3>Team Members</h3>
            <input id="edit-team-name" type="text" placeholder="Edit Team Name">
            <div id="team-members"></div>
            <h3>Add Students to Team </h3>
            <small>(make sure to click save)</small>
            <div id="additional-students"></div>
            <div style="margin-top: 10px;">
                <button id="save-and-close-team">Save and Close</button>
                <button id="delete-team">Delete Team</button>
            </div>
        </div>
    </div>
    <div id="team-list"></div>
    <div id="review-table"></div>
    <script src="teacherGrouping.js"></script>
</body>
</html>
