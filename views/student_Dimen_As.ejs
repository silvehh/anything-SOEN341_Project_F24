<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dimension-Based Assessment</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin: 0;
            height: 100vh;
            background-image: linear-gradient(to top, #708090, #778d95, #859a99, #97a59e, #aaafa7, #a7aca4, #a5aaa2, #a2a79f, #8a9690, #738585, #61747a, #55616e);
            color: #E0E0E0; /* Light grey text for readability */
        }

        .container {
            text-align: center;
            background-color: rgba(77, 77, 77, 0.95);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
            width: 400px;
        }

        .dimension {
            margin-bottom: 20px;
            text-align: left;
        }

        .dimension label {
            font-size: 16px;
            margin-bottom: 5px;
            display: block;
            font-weight: bold;
        }

        .dimension textarea {
            width: 100%; /* Ensures the textarea fills the container's width */
            height: 100px;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 16px;
            resize: vertical;
            background-color: #4D4D4D;
            color: #E0E0E0;
            box-sizing: border-box; /* Prevents overflow due to padding */
        }

        .btn {
            padding: 10px 20px;
            margin: 10px;
            border: none;
            background-color: #4F8A8B;
            color: white;
            cursor: pointer;
            font-size: 16px;
            border-radius: 5px;
            text-decoration: none;
        }

        .btn:hover {
            opacity: 0.9;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Further Evaluation of <%= evaluatee.name %></h2>
        <form action="/submit-dimensions" method="post">
            <input type="hidden" name="evaluateeEmail" value="<%= evaluatee.email %>">

            <div class="dimension">
                <label for="q1">Conceptual Contribution</label>
                <textarea id="q1" name="q1" placeholder="Write Here" required></textarea>
            </div>

            <div class="dimension">
                <label for="q2">Practical Contribution</label>
                <textarea id="q2" name="q2" placeholder="Write Here" required></textarea>
            </div>

            <div class="dimension">
                <label for="q3">Work Ethic</label>
                <textarea id="q3" name="q3" placeholder="Write Here" required></textarea>
            </div>

            <button class="btn" type="submit">Submit Assessment</button>
        </form>
    </div>
</body>
</html>
